import { makeStyles } from '@fluentui/react-components';

export const useAuthButtonStyles = makeStyles({
	userContainer: {
		display: 'flex',
		alignItems: 'center',
		gap: '8px',
	},
	userName: {
		fontSize: '14px',
	},
});
