export const workspaceMoveMessageBarLabels = {
	title: 'Workspace change detected',
	message:
		'It looks like this item was moved to a different workspace via CI/CD. Post-deployment setup will repoint the Semantic Model SQL endpoint, re-map deployed item links, and re-install sample data if needed.',
	button: 'Review & run setup',
};
