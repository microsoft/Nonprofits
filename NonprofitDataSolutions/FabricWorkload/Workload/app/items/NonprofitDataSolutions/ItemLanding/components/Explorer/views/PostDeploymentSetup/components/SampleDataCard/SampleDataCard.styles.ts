import { makeStyles, tokens } from '@fluentui/react-components';

export const useSampleDataCardStyles = makeStyles({
	content: {
		display: 'flex',
		flexDirection: 'column',
		gap: tokens.spacingVerticalXXS,
	},
});
